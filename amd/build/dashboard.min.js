/**
 * Player ecosystem dashboard interactions.
 *
 * Handles click (and Enter/Space keydown) on SVG plugin nodes.
 * Each click opens a core/modal populated with pre-rendered content
 * from a hidden <div id="pg-modal-{component}"> in the page.
 *
 * @module     local_playergames/dashboard
 * @copyright  2026 Jean Lúcio
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_playergames/dashboard",["core/modal"],(function(Modal){const openNodeModal=async node=>{const component=node.dataset.component,name=node.dataset.name,bodyEl=document.getElementById("pg-modal-"+component);bodyEl&&await Modal.create({title:name,body:bodyEl.innerHTML,show:!0,removeOnClose:!0})};return{init:()=>{document.querySelectorAll(".pg-ecosystem-node").forEach((node=>{node.addEventListener("click",(()=>openNodeModal(node))),node.addEventListener("keydown",(e=>{"Enter"!==e.key&&" "!==e.key||(e.preventDefault(),openNodeModal(node))}))}))}}}));

//# sourceMappingURL=dashboard.min.js.map